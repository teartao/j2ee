  
  <view class="operations" wx:if="{{menu!=null}}">
    <view>您的点餐结果:</view>
    <view style="color:red"> {{menu.name}}  ￥{{menu.price}}</view> 
        <image wx:for="{{imgalist}}" wx:for-item="image" wx:key="imgKey"
        src="{{image}}" data-src="{{image}}" bindtap="previewImage" />  
      <view class="textarea-wrp">复制口令去支付宝付款:【{{alipayCode}}】</view>      
      <button type="primary" class="operation-btn"
      bindtap="copyToClipBoard" >复制口令</button>
  </view>