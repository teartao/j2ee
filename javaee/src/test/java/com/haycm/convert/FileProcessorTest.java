/*
package com.haycm.convert;

import org.hehe.converters.Converter;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import javax.annotation.Resource;

*/
/**
 * @Author TaoLei
 * @Date 2015/4/21.
 *//*

@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration({"classpath:spring/applicationContext.xml"})
public class FileProcessorTest {
    @Resource
    @Qualifier("CobConverter")
    private Converter fileConvertUtils;

    @Test
    public void test1() {
        String htmlPath = "E:\\download\\temp\\test.html";
        String docPath = "E:\\download\\temp\\newDoc.doc";
        String docxPath = "E:\\download\\temp\\newDocx.docx";
        String pdfPath = "E:\\download\\temp\\newDocx.pdf";

        fileConvertUtils.convertHtmlToDOC(htmlPath, docPath);
        fileConvertUtils.convertHtmlToDOCX(htmlPath, docxPath);
        fileConvertUtils.convertWordToPDF(docPath, pdfPath);
        fileConvertUtils.convertHtmlToPDF(htmlPath, pdfPath);

    }
}
*/
